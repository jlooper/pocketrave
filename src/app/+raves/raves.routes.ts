import { Routes } from '@angular/router';
// app
import { RavesComponent } from './components/raves/raves.component';

export const RavesRoutes: Routes = [
    {
        path: '',
        component: RavesComponent
    }
];
